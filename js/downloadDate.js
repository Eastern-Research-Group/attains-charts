downloadDate =  '2024-07-31';

window.onload = function() {
   document.getElementById("downloadDate").innerHTML = 'Data in these charts were downloaded from the ATTAINS web services on ' + downloadDate + '.';
}